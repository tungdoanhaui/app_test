<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.smartconfigapp">

    <!-- Quyền cho Internet + SmartConfig -->
    <uses-sdk
        android:minSdkVersion="21"
        tools:overrideLibrary="com.google.android.libraries.ads.mobile.sdk" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Để requestNetwork() không bị SecurityException -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>



    <!-- Thêm các quyền mạng/wifi cơ bản -->

    <application

        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartConfigApp"
        tools:targetApi="31"
        android:usesCleartextTraffic="true"

        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SmartConfigApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".Screen01Activity"
            android:exported="false"
            android:label="screen01"
            android:theme="@style/Theme.SmartConfigApp"/>

        <activity
            android:name=".Screen02Activity"
            android:exported="false"
            android:label="screen02"
            android:theme="@style/Theme.SmartConfigApp"/>
    </application>

</manifest>
